NUXT_PUBLIC_API_BASE=/api/v1

# smspilot.ru API key (server-side only, never exposed to the client).
# Get an emulator key at https://smspilot.ru/ for testing — no real SMS is sent.
SMSPILOT_API_KEY=
